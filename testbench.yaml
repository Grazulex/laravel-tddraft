providers:
  - Grazulex\LaravelTddraft\LaravelTddraftServiceProvider

workbench:
  start: "/"
  user: 1
  
  install: true
  welcome: false
  
  build:
    - env-file: .env.example
    - migrate: true
    - seed: true
  
  assets:
    - from: "tests/Fixtures"
      to: "tests/Fixtures"
